import Link from "next/link";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { adminCookieName, verifyAdminToken } from "@/lib/admin-auth";
import PagesManager from "./pages-manager";
export default async function AdminPagesPage(){const store=await cookies();if(!verifyAdminToken(store.get(adminCookieName())?.value))redirect("/admin/login");return <main className="min-h-screen bg-[#f4f5e9] text-[#101510]"><header className="sticky top-0 z-40 border-b border-black/10 bg-[#f4f5e9]/90 px-5 py-4 backdrop-blur-xl sm:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4"><div><Link href="/" className="text-xs font-extrabold tracking-[.2em]">VERDANT</Link><p className="mt-1 text-[10px] font-bold tracking-[.16em] text-black/40">ADMIN / PAGES</p></div><div className="flex flex-wrap gap-2"><Link href="/admin/home" className="rounded-full border border-black/10 px-4 py-2 text-sm font-semibold">Home</Link><Link href="/admin/site" className="rounded-full border border-black/10 px-4 py-2 text-sm font-semibold">Site</Link><Link href="/admin/media" className="rounded-full border border-black/10 px-4 py-2 text-sm font-semibold">Media</Link><Link href="/admin/catalog" className="rounded-full border border-black/10 px-4 py-2 text-sm font-semibold">Catalog</Link><form action="/api/admin/logout" method="post"><button className="rounded-full bg-[#202d20] px-4 py-2 text-sm font-bold text-[#f4f5e9]">Log out</button></form></div></div></header><section className="mx-auto max-w-7xl px-5 py-10 sm:px-8 sm:py-14"><p className="text-[10px] font-black tracking-[.2em] text-[#315233]">PAGES & JOURNAL</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.055em] sm:text-6xl">Tell the <em className="font-serif font-normal">story.</em></h1><p className="mt-4 max-w-2xl text-sm leading-7 text-black/55">Create editable pages and journal content, keep drafts private, and publish when ready.</p><PagesManager/></section></main>}
